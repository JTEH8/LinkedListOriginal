#include <iostream>
#include "Node.h"

using namespace std;
void add(Node* currentNode,int newValue);
void print(Node* currentNode, Node* next);

int main(){
Node* head = new Node();

}

void add(Node* currentNode,int newValue){
    current
}